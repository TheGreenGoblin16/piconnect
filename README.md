### Bluetooth communication between computer and Rapbserry Pi Pico WH
- `bluetooth_client.py` runs on the computer. Using `bleak` library.
- `bluetooth_server.py` runs on the pico. Using `aioble` library.